{% for pokemon in feed.getLivePokemons() %}
{{ pokemon.lat }},{{ pokemon.lng }},{{ pokemon.pokedexNumber }},{{ pokemon.pokedexNumber|pokedexName }} {{ pokemon.iv }}iv CP{{ pokemon.cp }},Despawn: {{ pokemon.despawnTime|date('H:i:s') }}
{% endfor %}
