{% for pokemon in feed.getLivePokemons() %}
{{ pokemon.lat }},{{ pokemon.lng }},{{ pokemon.pokedexNumber }},{{ pokemon.pokedexNumber|pokedexName }}{{ pokemon.getStats() }},Despawn: {{ pokemon.despawnTime|date('H:i:s') }}
{% endfor %}
